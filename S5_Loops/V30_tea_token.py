'''
Loops:

2 main types of loops: "for" and "while"
Looping through sequences like "range()", "enumerate()" and "zip()"
Control loop behaviour using "break", "continue" and "else" clauses
When to use "for" vs "while" loop.

Note: In python the range(0, 11) the last element is never inclusuive. That means it goes from 0 to 10

'''

for i in range(1, 11):
    print(f"Serving token {i} now.")
